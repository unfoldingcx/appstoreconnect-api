# NPM Configuration for @unfoldingcx scope
# This ensures the package is published to the correct npm registry

# If you're using a custom registry, uncomment and configure:
# @unfoldingcx:registry=https://registry.npmjs.org/

# Default access level for scoped packages
access=public

# Optional: Set up npm authentication
# //registry.npmjs.org/:_authToken=${NPM_TOKEN}

